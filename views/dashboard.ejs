<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>لوحة تحكم THE BEST</title>
  <style>
    body { font-family: Arial, sans-serif; background-color: #1e1e2f; color: #fff; margin:0; padding:0; }
    header { background-color: #2c2c3e; padding: 20px; text-align: center; }
    main { padding: 20px; }
    .user-info { display: flex; align-items: center; gap: 15px; margin-bottom: 20px; }
    .user-info img { border-radius: 50%; width: 60px; height: 60px; }
    .guild { background-color: #2c2c3e; padding: 10px; margin: 10px 0; border-radius: 10px; }
    .logout { background-color: #ff5555; color: #fff; border: none; padding: 10px 20px; border-radius: 5px; cursor: pointer; text-decoration: none; }
    h2 { margin-top: 0; }
  </style>
</head>
<body>
  <header>
    <h1>لوحة تحكم THE BEST</h1>
  </header>
  <main>
    <div class="user-info">
      <img src="https://cdn.discordapp.com/avatars/<%= user.id %>/<%= user.avatar %>.png?size=128" alt="Avatar">
      <h2><%= user.username %>#<%= user.discriminator %></h2>
    </div>

    <h3>السيرفرات اللي انت عضو فيها:</h3>
    <% guilds.forEach(guild => { %>
      <div class="guild">
        <strong><%= guild.name %></strong> | ID: <%= guild.id %>
      </div>
    <% }) %>

    <a href="/logout" class="logout">تسجيل الخروج</a>
  </main>
</body>
</html>
